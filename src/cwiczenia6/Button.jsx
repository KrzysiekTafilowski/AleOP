import React from 'react';
import styles from '../style/Button.module.css'

const Button = () => {
    return (
        <div>
            <button className={styles.primary}>aha</button>
        </div>
    );
};

export default Button;